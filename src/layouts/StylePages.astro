---
import { AppShell, ContentShell } from ".";
import type { AppShellProps } from "../layouts/types";

type Props = Pick<AppShellProps, "title" | "status" | "barLabel">

const props = Astro.props;
---

<AppShell { ...props }>
  <ContentShell>
    <slot />
  </ContentShell>
</AppShell>